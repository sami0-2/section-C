# String Method isalpha()

The isalpha() method in Python is a built-in string method that checks if all characters in a string are alphabetic (letters A-Z or a-z) and contains at least one character.

## Syntax

string.isalpha()

## Parameters:
     None.


## Returns:
     True if all characters are alphabetic and the string is not empty.

     False otherwise.